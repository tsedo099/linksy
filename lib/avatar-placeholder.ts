/**
 * Fallback background when a user has no profile photo.
 * Use everywhere so initials-only avatars look the same (not per-user hues).
 */
export const AVATAR_PLACEHOLDER_GRADIENT =
  "linear-gradient(135deg, #6366f1 0%, #a855f7 100%)";
